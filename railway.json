{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfile": "Dockerfile",
    "buildContext": "."
  },
  "deploy": {
    "startCommand": "dotnet CafeEmployeeManager.Api.dll",
    "restartPolicyCondition": "on-failure",
    "restartPolicyMaxRetryCount": 5
  }
}
